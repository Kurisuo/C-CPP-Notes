## CODE ##

//
#include <thread>
#include <deque>
#include <mutex>
#include <chrono>
#include <condition_variable>
#include <random>
//




using std::deque; 
    //Allows us to create a sequence container which grants us fast insertion and deletion at both ends 
    //Double Ended Queue | O(1) Time | Utilizes Fixed-size memory blocks managed by a central map